server:
  port: 8089
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/microuser
    username: postgres
    password: postgress123

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
